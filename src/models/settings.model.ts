
export interface AppSettings {
  schoolName: string;
  accountantName: string;
  academicYear: string; // Just a display label now
  principalName: string;
  schoolLogo: string | null; // base64 string
  showCurrencySymbol: boolean;
}
